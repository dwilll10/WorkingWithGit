' Print bat file contents to Immediate Window
Dim checkFile As Integer
Dim checkLine As String
checkFile = FreeFile
Open batPath For Input As #checkFile
Do While Not EOF(checkFile)
    Line Input #checkFile, checkLine
    Debug.Print checkLine
Loop
Close #checkFile
