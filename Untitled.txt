Dim stmtMsg As String
    Dim stmtMsgFull As String
    
    If stmtDiscrepancies = -1 Then
        stmtMsg = vbNewLine & "Discrepancies in Statement to Statement: Column 'Difference' not found"
        stmtMsgFull = stmtMsg
    ElseIf Not stmtExists Then
        stmtMsg = vbNewLine & "Discrepancies in Statement to Statement: Tab 'STMT to STMT' not found"
        stmtMsgFull = stmtMsg
    ElseIf stmtDiscrepancies = 0 Then
        stmtMsg = vbNewLine & "Discrepancies in Statement to Statement: 0"
        stmtMsgFull = stmtMsg
    Else
        stmtMsg = vbNewLine & "Discrepancies in Statement to Statement: " & stmtDiscrepancies
        stmtMsgFull = stmtMsg & vbNewLine & vbNewLine & "Discrepancy detail:" & vbNewLine & discrepancyList
    End If

Dim fullMsg As String
    Dim fullMsgFile As String

    fullMsg = "Start time: " & Format(startTime, "yyyy-mm-dd hh:mm:ss") & vbNewLine & _
              "End time:   " & Format(endTime, "yyyy-mm-dd hh:mm:ss") & vbNewLine & _
              "Duration:   " & Format(endTime - startTime, "hh:mm:ss") & vbNewLine & vbNewLine & _
              statusMsg & tabsMsg & createdMsg & missingMsg & statsMsg & stmtMsg & noteMsg & vbNewLine & vbNewLine & _
              "Log file: " & errorLogPath

    fullMsgFile = "Start time: " & Format(startTime, "yyyy-mm-dd hh:mm:ss") & vbNewLine & _
                  "End time:   " & Format(endTime, "yyyy-mm-dd hh:mm:ss") & vbNewLine & _
                  "Duration:   " & Format(endTime - startTime, "hh:mm:ss") & vbNewLine & vbNewLine & _
                  statusMsg & tabsMsg & createdMsg & missingMsg & statsMsg & stmtMsgFull & noteMsg & vbNewLine & vbNewLine & _
                  "Log file: " & errorLogPath

summaryFile = FreeFile
    Open summaryPath For Output As #summaryFile
    Print #summaryFile, fullMsgFull
    Close #summaryFile

    fullMsg = fullMsg & vbNewLine & "Summary file: " & summaryPath

    If result <> 0 Then
        MsgBox fullMsg, vbCritical, "Script Failed"
    Else
        MsgBox fullMsg, vbInformation, "Execution Completed"
    End If
