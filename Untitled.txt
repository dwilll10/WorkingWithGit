Sub WriteLog(msg As String, logPath As String)
    Dim fileNum As Integer
    fileNum = FreeFile
    Open logPath For Append As #fileNum
    Print #fileNum, Now() & " | " & msg
    Close #fileNum
End Sub
