tabName = 'STMT to STMT'
            if tie_sheets_all_existed and tabName in existing_sheets:  # <
                logger.info(f"Skipping '{tabName}' — all tie-through sheets already exist.")  # <
            else:
                t0=time.time()
                fsFn = stmt.fs_to_fn(fsTofn)
                fnFn = stmt.fn_to_fn(fnTofn)
                fsFs = stmt.fs_to_fs(fsTofs)
                inte = stmt.internal_tie(internal_data)
                main = stmt.stmt_tie(fsFn, fnFn, fsFs, inte)
                logger.info(f"main: {len(main)} rows")
                sheet = df_to_sheet(main, tabName, wb, skip_if_exists=False)
                logger.info(f"TIMING: STMT TO STMT = {time.time()-t0:.2f}s")






import Tie_bs_fn as tie
import Tie_fn_fn as tie_fn
import Tie_fs_fs as tie_fs
import internal_fn as internal
import xlwings as xw
import pandas as pd
import mainScript as mn
import numpy as np
import file_paths as dir_p
import robust_utils as ru
logger = ru.logger


# ----------------------------------------
# Generalized reshaping function for any tie type
# ----------------------------------------

def reshape_tie(main, titles, fallbacks, amounts, section_label, report_names=None):
    """
    General function to reshape tie-through data into a uniform format.
    
    Parameters:
    - main (DataFrame): The input data.
    - titles (list): Two column names for the line titles.
    - fallbacks (list): Columns to fall back to if title values are missing.
    - amounts (list): Two column names for the amount values.
    - section_label (str): Label to identify the section type (e.g., 'FS to FN').
    - report_names (list, optional): Report name columns to prefix to titles.
    
    Returns:
    - reshaped_df (DataFrame): The reshaped, sectioned data.
    """
    try:
        # Fill missing titles with fallback values, optionally add report names
        for i in range(2):
            title_col = titles[i]
            fallback_col = fallbacks[i]
            report_col = report_names[i] if report_names else None

            # Use fallback if title is empty
            main[title_col] = np.where(
                main[title_col].fillna('').str.strip() == '',
                main[fallback_col],
                main[title_col]
            )

            # Prepend report name if provided
            if report_col:
                main[title_col] = main[report_col].fillna('') + ' - ' + main[title_col]

        # Keep only the relevant columns for reshaping
        diff_col = 'Difference'
        selected_cols = titles + amounts + [diff_col]
        main = main[selected_cols]
        

        reshaped_rows = []
        for _, row in main.iterrows():
            # Add FS or FN lines, difference line, and spacing
            reshaped_rows.append(["", "", ""])
            reshaped_rows.append([row[titles[0]], row[amounts[0]], ""])
            reshaped_rows.append([row[titles[1]], row[amounts[1]], ""])
            reshaped_rows.append(["", "", row[diff_col]])
            reshaped_rows.append(["", "", ""])

        # Create final reshaped DataFrame
        reshaped_df = pd.DataFrame(reshaped_rows, columns=["STATEMENT LINE NUMBER", "Amount", "Difference"])

        # Add empty column for user comments
        reshaped_df['COMMENTS/EXPLANATIONS'] = ''

        # Add section header rows
        blank_row = pd.DataFrame([["", "", "", ""]], columns=reshaped_df.columns)
        section_header = pd.DataFrame([[section_label, "", "", ""]], columns=reshaped_df.columns)

        # Concatenate: blank row → section label → blank row → data
        reshaped_df = pd.concat([blank_row, section_header, reshaped_df], ignore_index=True)

        return reshaped_df

    except Exception as e:
        logger.error(f"Error in reshape_tie: {e}")
        return None

# ----------------------------------------
# Wrapper functions for specific tie types
# ----------------------------------------

def fs_to_fn(main):
    return reshape_tie(
        main,
        titles=['FS_Line_Title', 'FN_Line_Title'],
        fallbacks=['FS_Line', 'FN_Line'],
        amounts=['FS Amount', 'FN Amount'],
        section_label='Tie-Through FS to FN',
        report_names=['FS_Report_Name', 'FN_Report_Name']
    )

def fn_to_fn(main):
    return reshape_tie(
        main,
        titles=['FN_Line_Title_One', 'FN_Line_Title_Two'],
        fallbacks=['FN_Line_One', 'FN_Line_Two'],
        amounts=['FN Amount One', 'FN Amount Two'],
        section_label='Tie-Through FN to FN',
        report_names=['FN_Report_Name_One', 'FN_Report_Name_Two']
    )

def fs_to_fs(main):
    return reshape_tie(
        main,
        titles=['FS_Line_Title_One', 'FS_Line_Title_Two'],
        fallbacks=['FS_Line_One', 'FS_Line_Two'],
        amounts=['FS Amount One', 'FS Amount Two'],
        section_label='Tie-Through FS to FS',
        report_names=['FS_Report_Name_One', 'FS_Report_Name_Two']
    )

def internal_tie(main):
    return reshape_tie(
        main,
        titles=['Line_Title_One', 'Line_Title_Two'],
        fallbacks=['Line_One', 'Line_Two'],
        amounts=['RN Amount One', 'RN Amount Two'],
        section_label='Internal Tie-Through',
        report_names=['Report_Name', 'Report_Name']  # Same report name used for both sides
    )

# ----------------------------------------
# Combine all reshaped tie DataFrames
# ----------------------------------------

def stmt_tie(*sections):
    """
    Concatenates all reshaped tie-through sections into one DataFrame.
    """
    return pd.concat(sections, ignore_index=True)

# ----------------------------------------
# Main script logic
# ----------------------------------------

if __name__ == "__main__":    
    # Open workbook
    app = xw.apps.active
    wb = app.books.open(dir_p.gk_template())

    # Extract raw data from Excel sheets using external modules
    fsTofn = tie.tie_through('Tie-Through', wb)
    fnTofn = tie_fn.tie_through('Tie-Through-fn to fn', wb)
    fsTofs = tie_fs.tie_through('Tie-Through fs to fs', wb)
    internal_data = internal.tie_through('Tie-Through Internal', wb)

    # Reshape each section
    fsFn = fs_to_fn(fsTofn)
    fnFn = fn_to_fn(fnTofn)
    fsFs = fs_to_fs(fsTofs)
    inte = internal_tie(internal_data)

    # Combine all reshaped sections
    main = stmt_tie(fsFn, fnFn, fsFs, inte)

    # Write final combined DataFrame to Excel
    mn.df_to_sheet(main, 'STMT to STMT', wb)










# <<< debug — remove once confirmed working
                for name, df in [('fsTofn', fsTofn), ('fnTofn', fnTofn), ('fsTofs', fsTofs), ('internal_data', internal_data)]:
                    logger.info(f"Read back '{name}': {len(df)} rows, columns: {list(df.columns)}")
                # <


tabName = 'STMT to STMT'
            tie_sheets_all_existed = all(df.empty for df in [fsTofn, fnTofn, fsTofs, internal_data])  # <

            if tie_sheets_all_existed and tabName not in existing_sheets:  # <
                # tie-through sheets were skipped but STMT to STMT is missing — read back from workbook  # <
                logger.info("Tie-through sheets exist but STMT to STMT is missing — reading from workbook sheets.")  # <
                fsTofn       = wb.sheets['Tie-Through'].range('A1').options(pd.DataFrame, expand='table', header=True).value  # <
                fnTofn       = wb.sheets['Tie-Through-fn to fn'].range('A1').options(pd.DataFrame, expand='table', header=True).value  # <
                fsTofs       = wb.sheets['Tie-Through fs to fs'].range('A1').options(pd.DataFrame, expand='table', header=True).value  # <
                internal_data = wb.sheets['Tie-Through Internal'].range('A1').options(pd.DataFrame, expand='table', header=True).value  # <
                tie_sheets_all_existed = False  # now we have data, let the block below run  # <

            if tie_sheets_all_existed and tabName in existing_sheets:
                logger.info(f"Skipping '{tabName}' — all tie-through sheets already exist.")
            else:
                t0=time.time()
                fsFn = stmt.fs_to_fn(fsTofn)
                fnFn = stmt.fn_to_fn(fnTofn)
                fsFs = stmt.fs_to_fs(fsTofs)
                inte = stmt.internal_tie(internal_data)
                main = stmt.stmt_tie(fsFn, fnFn, fsFs, inte)
                logger.info(f"main: {len(main)} rows")
                sheet = df_to_sheet(main, tabName, wb, skip_if_exists=False)
                logger.info(f"TIMING: STMT TO STMT = {time.time()-t0:.2f}s")




tabName = 'STMT to STMT'
            tie_sheets_all_existed = all(df.empty for df in [fsTofn, fnTofn, fsTofs, internal_data])  # <

            if tie_sheets_all_existed and tabName in existing_sheets:  # <
                logger.info(f"Skipping '{tabName}' — all tie-through sheets already exist.")  # <
            else:  # <
                t0=time.time()
                fsFn = stmt.fs_to_fn(fsTofn)
                fnFn = stmt.fn_to_fn(fnTofn)
                fsFs = stmt.fs_to_fs(fsTofs)
                inte = stmt.internal_tie(internal_data)
                main = stmt.stmt_tie(fsFn, fnFn, fsFs, inte)
                logger.info(f"main: {len(main)} rows")
                sheet = df_to_sheet(main, tabName, wb, skip_if_exists=False)
                logger.info(f"TIMING: STMT TO STMT = {time.time()-t0:.2f}s")
                           ^
IndentationError: unindent does not match any outer indentation level


tabName = 'Tie-Through'
            if tabName not in existing_sheets:  # <
                t0=time.time()
                fsTofn = Tie_bs_fn.tie_through(tabName, wb)
                logger.info(f"TIMING: Tie_bs_fn.tie_through = {time.time()-t0:.2f}s")
                t0=time.time()
                write_tie_sheet(tabName, fsTofn)
                logger.info(f"TIMING: Write tie through sheet = {time.time()-t0:.2f}s")
            else:                               # <
                logger.info(f"Skipping '{tabName}' — sheet already exists.")  # <
                fsTofn = pd.DataFrame()         # <

            tabName = 'Tie-Through-fn to fn'
            if tabName not in existing_sheets:  # <
                t0=time.time()
                fnTofn = Tie_fn_fn.tie_through(tabName, wb)
                logger.info(f"TIMING: Tie_fn_fn.tie_through = {time.time()-t0:.2f}s")
                t0=time.time()
                write_tie_sheet(tabName, fnTofn)
                logger.info(f"TIMING: Write fn-fn tie through = {time.time()-t0:.2f}s")
            else:                               # <
                logger.info(f"Skipping '{tabName}' — sheet already exists.")  # <
                fnTofn = pd.DataFrame()         # <

            tabName = 'Tie-Through fs to fs'
            if tabName not in existing_sheets:  # <
                t0=time.time()
                fsTofs = Tie_fs_fs.tie_through(tabName, wb)
                logger.info(f"TIMING: Tie_fs_fs.tie_through = {time.time()-t0:.2f}s")
                t0=time.time()
                write_tie_sheet(tabName, fsTofs)
                logger.info(f"TIMING: Write fs-fs tie through = {time.time()-t0:.2f}s")
            else:                               # <
                logger.info(f"Skipping '{tabName}' — sheet already exists.")  # <
                fsTofs = pd.DataFrame()         # <

            tabName = 'Tie-Through Internal'
            if tabName not in existing_sheets:  # <
                t0=time.time()
                internal_data = internal_fn.tie_through(tabName, wb)
                logger.info(f"TIMING: Tie_internal.tie_through = {time.time()-t0:.2f}s")
                t0=time.time()
                write_tie_sheet(tabName, internal_data)
                logger.info(f"TIMING: Write internal tie through = {time.time()-t0:.2f}s")
            else:                               # <
                logger.info(f"Skipping '{tabName}' — sheet already exists.")  # <
                internal_data = pd.DataFrame()  # <


















def get_pending_fn_filters(existing_sheets: set[str], wb) -> list[str]:
    """
    Returns the list of fn_filter keys whose output sheet is NOT yet in the workbook.
    Uses the same Config mapping that import_fn/import_fs already reads.
    """
    # Load the same config import_fn uses
    mapping_file = dir_p.mapping_table()
    df_fn  = pd.read_excel(mapping_file, sheet_name='Config')
    df_fs  = pd.read_excel(mapping_file, sheet_name='Config-FS')

    fn_keys = df_fn['report_key'].str.strip().str.lower().tolist()   # e.g. ['fn2', 'fn3', ...]
    fs_keys = df_fs['report_key'].str.strip().str.lower().tolist()   # e.g. ['bs', 'snc', ...]

    # Map fn_filter key → output sheet name in the workbook
    # The sheet name is the uppercased key, matching your fn_names list
    # e.g. 'fn2' → 'FN2', 'rsi-deferred' → 'RSI-DEFERRED', 'bs' → 'BS'
    def to_sheet_name(key: str) -> str:
        return key.upper()

    pending_fn = [k for k in fn_keys if to_sheet_name(k) not in existing_sheets]
    pending_fs = [k for k in fs_keys if to_sheet_name(k) not in existing_sheets]

    return pending_fn, pending_fs





def updateFile(active_sheet_name='Start'):
    try:
        overall_start = time.time()
        app = xw.apps.active

        matching_books = [
            wb for wb in app.books
            if active_sheet_name in [sh.name for sh in wb.sheets]
        ]

        if len(matching_books) == 0:
            raise ValueError(f"No open workbook contains a sheet named '{active_sheet_name}'.")
        elif len(matching_books) > 1:
            names = [wb.name for wb in matching_books]
            raise ValueError(
                f"Multiple open workbooks contain a sheet named '{active_sheet_name}': {names}. "
                f"Please close the others and try again."
            )

        wb = matching_books[0]
        logger.info(f"Using active workbook: {wb.name}")

        try:
            logger.info(f"Workbook fullname: {wb.fullname}")
        except Exception:
            pass

        # <<< Pre-flight skip check
        existing_sheets = {sh.name for sh in wb.sheets}
        pending_fn, pending_fs = get_pending_fn_filters(existing_sheets, wb)
        if not pending_fn and not pending_fs:
            logger.info("All sheets already present — nothing to process.")
            return
        logger.info(f"Pending FN filters: {pending_fn}")
        logger.info(f"Pending FS filters: {pending_fs}")
        # <

        app.screen_updating = False
        app.calculation = 'manual'
        app.display_alerts = False
        app.api.EnableEvents = False
    
        try:
            if active_sheet_name in [sheet.name for sheet in wb.sheets]:
                wb.sheets[active_sheet_name].activate()       
    
            if active_sheet_name in [sheet.name for sheet in wb.sheets]:
                wb.sheets[active_sheet_name].activate()
    
            def write_tie_sheet(tab_name, df):
                if df.empty:
                    logger.error(f"Error: {tab_name} returned an empty DataFrame.")
                    return
                sheet = df_to_sheet(df, tab_name, wb, skip_if_exists=True)
                if sheet is not None:
                    formatResult(sheet, df)
                else:
                    logger.info(f"Skipped formatting for existing sheet '{tab_name}'.")
    
            tabName = 'Tie-Through'
            t0=time.time()
            fsTofn = Tie_bs_fn.tie_through(tabName, wb)
            logger.info(f"TIMING: Tie_bs_fn.tie_through = {time.time()-t0:.2f}s")
            t0=time.time()
            write_tie_sheet(tabName, fsTofn)
            logger.info(f"TIMING: Write tie through sheet = {time.time()-t0:.2f}s")

            tabName = 'Tie-Through-fn to fn'
            t0=time.time()
            fnTofn = Tie_fn_fn.tie_through(tabName, wb)
            logger.info(f"TIMING: Tie_fn_fn.tie_through = {time.time()-t0:.2f}s")
            t0=time.time()
            write_tie_sheet(tabName, fnTofn)
            logger.info(f"TIMING: Write fn-fn tie through = {time.time()-t0:.2f}s")

            tabName = 'Tie-Through fs to fs'
            t0=time.time()
            fsTofs = Tie_fs_fs.tie_through(tabName, wb)
            logger.info(f"TIMING: Tie_fs_fs.tie_through = {time.time()-t0:.2f}s")
            t0=time.time()
            write_tie_sheet(tabName, fsTofs)
            logger.info(f"TIMING: Write fs-fs tie through = {time.time()-t0:.2f}s")

            tabName = 'Tie-Through Internal'
            t0=time.time()
            internal_data = internal_fn.tie_through(tabName, wb)
            logger.info(f"TIMING: Tie_internal.tie_through = {time.time()-t0:.2f}s")
            t0=time.time()
            write_tie_sheet(tabName, internal_data)
            logger.info(f"TIMING: Write internal tie through = {time.time()-t0:.2f}s")
            
            function_calls = [
                lambda: RR_updateFn.rsiDeferred('RSI-DEFERRED', wb) if 'rsi-deferred' in pending_fn else None,  # <
                lambda: RR_updateFn.rsiSbr('RSI-SBR', wb)           if 'rsi-sbr'      in pending_fn else None,  # <
                lambda: RR_updateFn.oiSos('OI-SOS', wb)             if 'oi-sos'        in pending_fn else None,  # <
                lambda: RR_updateFn.rsiLand('RSI-LAND', wb)         if 'rsi-land'     in pending_fn else None,  # <
                lambda: RR_updateFn.RsiSca('RSI-SCA', wb)           if 'rsi-sca'      in pending_fn else None,  # <
                lambda: RR_updateFn.fn32('FN32', wb)                 if 'fn32'         in pending_fn else None,  # <
                lambda: RR_updateFn.fn30Scnp('FN30SCNP', wb)        if 'fn30scnp'     in pending_fn else None,  # <
                lambda: RR_updateFn.fn30Snc('FN30SNC', wb)          if 'fn30snc'      in pending_fn else None,  # <
                lambda: RR_updateFn.fn29('FN29', wb)                 if 'fn29'         in pending_fn else None,  # <
                lambda: RR_updateFn.fn28('FN28', wb)                 if 'fn28'         in pending_fn else None,  # <
                lambda: RR_updateFn.fn27('FN27', wb)                 if 'fn27'         in pending_fn else None,  # <
                lambda: RR_updateFn.fn26('FN26', wb)                 if 'fn26'         in pending_fn else None,  # <
                lambda: RR_updateFn.fn25('FN25', wb)                 if 'fn25'         in pending_fn else None,  # <
                lambda: RR_updateFn.fn23('FN23', wb)                 if 'fn23'         in pending_fn else None,  # <
                lambda: RR_updateFn.fn22('FN22', wb)                 if 'fn22'         in pending_fn else None,  # <
                lambda: RR_updateFn.fn21('FN21', wb)                 if 'fn21'         in pending_fn else None,  # <
                lambda: RR_updateFn.fn20('FN20', wb)                 if 'fn20'         in pending_fn else None,  # <
                lambda: RR_updateFn.fn19('FN19', wb)                 if 'fn19'         in pending_fn else None,  # <
                lambda: RR_updateFn.fn18('FN18', wb)                 if 'fn18'         in pending_fn else None,  # <
                lambda: RR_updateFn.fn17('FN17', wb)                 if 'fn17'         in pending_fn else None,  # <
                lambda: RR_updateFn.fn16('FN16', wb)                 if 'fn16'         in pending_fn else None,  # <
                lambda: RR_updateFn.fn15('FN15', wb)                 if 'fn15'         in pending_fn else None,  # <
                lambda: RR_updateFn.fn14('FN14', wb)                 if 'fn14'         in pending_fn else None,  # <
                lambda: RR_updateFn.fn13('FN13', wb)                 if 'fn13'         in pending_fn else None,  # <
                lambda: RR_updateFn.fn12('FN12', wb)                 if 'fn12'         in pending_fn else None,  # <
                lambda: RR_updateFn.fn11('FN11', wb)                 if 'fn11'         in pending_fn else None,  # <
                lambda: RR_updateFn.fn10('FN10', wb)                 if 'fn10'         in pending_fn else None,  # <
                lambda: RR_updateFn.fn9('FN9', wb)                   if 'fn9'          in pending_fn else None,  # <
                lambda: RR_updateFn.fn8('FN8', wb)                   if 'fn8'          in pending_fn else None,  # <
                lambda: RR_updateFn.fn7('FN7', wb)                   if 'fn7'          in pending_fn else None,  # <
                lambda: RR_updateFn.fn6('FN6', wb)                   if 'fn6'          in pending_fn else None,  # <
                lambda: RR_updateFn.fn5('FN5', wb)                   if 'fn5'          in pending_fn else None,  # <
                lambda: RR_updateFn.fn4('FN4', wb)                   if 'fn4'          in pending_fn else None,  # <
                lambda: RR_updateFn.fn3('FN3', wb)                   if 'fn3'          in pending_fn else None,  # <
                lambda: RR_updateFn.fn2('FN2', wb)                   if 'fn2'          in pending_fn else None,  # <
                lambda: RR_updateFs.scaFormat('SCA', wb)             if 'sca'          in pending_fs else None,  # <
                lambda: RR_updateFs.sbrFormat('SBR', wb)             if 'sbr'          in pending_fs else None,  # <
                lambda: RR_updateFs.scnpFormat('SCNP', wb)           if 'scnp'         in pending_fs else None,  # <
                lambda: RR_updateFs.sncFormat('SNC', wb)             if 'snc'          in pending_fs else None,  # <
                lambda: RR_updateFs.bsFormat('BS', wb)               if 'bs'           in pending_fs else None,  # <
            ]

            # rest of function unchanged from here...





























import os
import re
import pandas as pd
import file_paths as dir_p
import warnings
import cProfile
import robust_utils as ru

logger = ru.logger

#warninig message will not not be displayed in the console
warnings.filterwarnings("ignore", message="Data Validation extension is not supported")
 
mapping_file = dir_p.mapping_table()           
                
def import_fn(fn_filter):
    # Define the directory path containing your files
    directory_path = dir_p.fn_path()
    
    df = pd.read_excel(mapping_file, sheet_name='Config')
    files = dict(zip(df['report_key'].str.strip(), df['source_tab'].str.strip()))
    
    
    #Define the files dictionary
    # files = {
    #     'fn2': 'ENEAssets',
    #     'fn3': 'FBWT',
    #     'fn4': 'CashMonAssets',
    #     'fn5': 'Investments',
    #     'fn5_t': 'NARRDATA - AFR RF',
    #     'fn6': 'AcctRec',
    #     'fn6_t': 'FN6-AFR RF',
    #     'fn7': 'TaxRcvbls',
    #     'fn8': 'FN8 AFR RF',
    #     'fn9': 'Inventory',
    #     'fn10': 'FN10-AFR RF',
    #     'fn11': 'FN11-AFR RF',
    #     'fn12': 'FN12-AFR RF', 
    #     'fn13': 'OtherAssets',
    #     'fn14': 'CovUncov',
    #     'fn15': 'FN15 - Debt',
    #     'fn16': 'FN 16 AFR RF',
    #     'fn17': 'FY25 EnvDispLiabs',
    #     'fn18': 'FN18-AFR RF',
    #     'fn19': 'Roll-Forward',
    #     'fn20': 'FN20-AFR RF',
    #     'fn21': 'CLL ARF',
    #     'fn21_t': 'FN21 Funded NarrData',
    #     'fn22': 'FN22 ',
    #     'fn23': 'AvailBorrow ',
    #     'fn25': 'FN25 AFR',
    #     'fn26': 'UndelivOrd',
    #     'fn27': 'AFR Note',
    #     'fn28a': 'FN28A - AFR RF',
    #     'fn28b': 'FN28B - AFR RF',
    #     'fn29': 'AFR Presentation ',
    #     'fn30snc': 'FN30 Roll-Forward',
    #     'fn30scnp': 'FN 30',
    #     'fn32': 'FN32 - AFR RF',
    #     'oi-sos': 'SOS boc by component',
    #     #'oi-sos': 'SOS   AFR',
    #     'rsi-deferred': 'RSI-Deferred AFR RF',
    #     'rsi-land': 'PP&E Land Totals',
    #     'rsi-land_t': 'Stewardship Land Totals',
    #     'rsi-sca': 'NARRDATA - AFR RF',
    #     'rsi-sbr': 'AFR RF'
    # }
    #print(files)
    # Create a list to hold FY, QTR, FN (File Number), Tab, and file name pairs
    data = []
    
   
    # Iterate through files in the directory
    for filename in os.listdir(directory_path):
        full_path = os.path.join(directory_path, filename)

        # Check if it's a file (not a directory)
        if os.path.isfile(full_path):
            # Try to find the Fiscal Year (FY) and Quarter (QTR) using regex
            fy_qtr_match = re.search(r'FY(\d{2})\s*PD(\d{1,2})', filename)
            if fy_qtr_match:
                fy = fy_qtr_match.group(1)  # Extract FY (e.g., "24")
                qtr = fy_qtr_match.group(2)  # Extract QTR (e.g., "9" or "12")
            else:
                fy, qtr = None, None  # Default if not found

            # Try to find FN number using regex (e.g., FN30S, FN30SCNP, FN30SNC, etc.)
            fn_match = re.search(r'FN\w+', filename)
            
            if fn_match:
                fn_number = fn_match.group(0).lower()  # Convert FN to lowercase
              
                # Only process if the fn_number matches the fn_filter
                if fn_number.startswith(fn_filter) and (len(fn_number) == len(fn_filter) or not fn_number[len(fn_filter)].isdigit()):
                    
                #if fn_filter in fn_number:
                    tab = files.get(fn_filter, None)  # Get tab from the files dictionary, if exists
                    if tab:
                        data.append((fy, qtr, fn_filter, tab, filename))
                
                # Check if FN is one of the special cases (fn5_t, fn6_t, fn21_t)
                # Add the case where you are looking for 'fn5_t' separately
                if fn_number.startswith('fn5') and fn_filter == 'fn5_t':
                    print('fn5_t')
                    tab = files.get(fn_filter, None)  # Get tab from the files dictionary, if exists
                    if tab:
                        data.append((fy, qtr, fn_filter, tab, filename))
                    #data.append((fy, qtr, 'fn5_t', 'NARRDATA - AFR RF', filename))
                elif fn_number.startswith('fn6') and fn_filter == 'fn6_t':
                    print('fn6-t')
                    tab = files.get(fn_filter, None)  # Get tab from the files dictionary, if exists
                    if tab:
                        data.append((fy, qtr, fn_filter, tab, filename))
                    #data.append((fy, qtr, 'fn6_t', 'FN6-AFR RF', filename))
                elif fn_number.startswith('fn21') and fn_filter == 'fn21_t':
                    print('fn21-t')
                    tab = files.get(fn_filter, None)  # Get tab from the files dictionary, if exists
                    if tab:
                        data.append((fy, qtr, fn_filter, tab, filename))
                    #data.append((fy, qtr, 'fn21_t', 'FN21 Funded NarrData', filename))

            # Check if RSI term is in the filename
            rsi_match = re.search(r'RSI[- ]?([^ ]+)', filename)
            if rsi_match:
                rsi_fn = rsi_match.group(0).replace('.xlsx', '').lower()  # Remove .xlsx and convert to lowercase
                tab = files.get(rsi_fn, None)  # Get tab from the files dictionary, if exists
                if tab and rsi_fn == fn_filter:
                    data.append((fy, qtr, rsi_fn, tab, filename))
                    
                if rsi_fn == 'rsi-land' and fn_filter == 'rsi-land_t':
                    tab = files.get(fn_filter, None)  # Get tab from the files dictionary, if exists
                    if tab:
                        data.append((fy, qtr, fn_filter, tab, filename))
                    #data.append((fy, qtr, 'rsi-land_t', 'Stewardship Land Totals', filename))

            # Check if OI-SOS term is in the filename
            oi_sos_match = re.search(r'OI-SOS', filename)
            if oi_sos_match:
                oi_sos_fn = oi_sos_match.group(0).lower()  # Convert OI-SOS to lowercase
                if fn_filter in oi_sos_fn:
                #if oi_sos_fn == fn_filter:
                    tab = files.get(fn_filter, None)  # Get tab from the files dictionary, if exists
                    if tab:
                        data.append((fy, qtr, fn_filter, tab, filename))
                    #data.append((fy, qtr, oi_sos_fn, 'SOS boc by component', filename))
   
    # Create a DataFrame from the list of tuples (FY, QTR, FN, Tab, File Name)
    df = pd.DataFrame(data, columns=['FY', 'QTR', 'FN', 'Tab', 'File Name'])

   
    # Now, load the Excel file using the file name and the sheet name (Tab)
    dfs = []  # This will hold all DataFrames that match the file name and tab
    for _, row in df.iterrows():
        file_path = os.path.join(directory_path, row['File Name'])
        sheet_name = row['Tab']
        
        # Use flexible sheet name matching to handle trailing spaces, case changes, etc.
        try:
            actual_sheet = ru.find_sheet_name(file_path, sheet_name)
            if actual_sheet != sheet_name:
                logger.info("Sheet name normalised: '%s' -> '%s' in %s",
                            sheet_name, actual_sheet, file_path)
            sheet_df = pd.read_excel(file_path, sheet_name=actual_sheet, engine='openpyxl')
            if sheet_df.empty:
                logger.warning("Sheet '%s' in %s is empty.", actual_sheet, file_path)
            dfs.append(sheet_df)
        except ValueError as e:
            logger.error("Sheet lookup failed for '%s' in %s: %s", sheet_name, file_path, e)
        except Exception as e:
            logger.error("Error loading file %s, sheet %s: %s", file_path, sheet_name, e)

    # Combine all DataFrames into a single one (optional)
    final_df = pd.concat(dfs, ignore_index=True) if dfs else pd.DataFrame()

    if final_df.empty:
        logger.warning("import_fn('%s') returned an empty DataFrame. Check file paths and sheet names.", fn_filter)

    return final_df


# def import_fs(fn_filter):
#     # Define the directory path containing your files
#     directory_path = dir_p.fs_path()
    

#     # Create a list to hold FY, QTR, FN (File Number), Tab, and file name pairs
#     data = []

#     # Iterate through files in the directory
#     for filename in os.listdir(directory_path):
#         full_path = os.path.join(directory_path, filename)
        
#         # Check if it's a file (not a directory)
#         if os.path.isfile(full_path):
#             # Try to find the Fiscal Year (FY) and Quarter (QTR) using regex
#             fy_qtr_match = re.search(r'FY(\d{2})\s*PD(\d{1,2})', filename)
#             if fy_qtr_match:
#                 fy = fy_qtr_match.group(1)  # Extract FY (e.g., "24")
#                 qtr = fy_qtr_match.group(2)  # Extract QTR (e.g., "9" or "12")
#             else:
#                 fy, qtr = None, None  # Default if not found


#             # Check if OI-SOS term is in the filename
#             bs_match = re.search(r'BS', filename)
#             if bs_match:
#                 bs_fn = bs_match.group(0).lower()  # Convert OI-SOS to lowercase
#                 if bs_fn == fn_filter:
#                     data.append((fy, qtr, bs_fn, 'BalanceSheet', filename))
                    
#             # Check if SNC term is in the filename
#             snc_match = re.search(r'SNC', filename)
#             if snc_match:
#                 snc_fn = snc_match.group(0).lower()  # Convert OI-SOS to lowercase
#                 if snc_fn == fn_filter:
#                     data.append((fy, qtr, snc_fn, 'SNC (AFR)', filename))
                    
#             # Check if SNC term is in the filename
#             scnp_match = re.search(r'SCNP', filename)
#             if scnp_match:
#                 scnp_fn = scnp_match.group(0).lower()  # Convert OI-SOS to lowercase
#                 if scnp_fn == fn_filter:
#                     data.append((fy, qtr, scnp_fn, 'SCNP8', filename))
                    
#             # Check if SNC term is in the filename
#             sca_match = re.search(r'SCA', filename)
#             if sca_match:
#                 sca_fn = sca_match.group(0).lower()  # Convert OI-SOS to lowercase
#                 if sca_fn == fn_filter:
#                     data.append((fy, qtr, sca_fn, 'CustodialActivity', filename))
                    
#             # Check if SNC term is in the filename
#             sbr_match = re.search(r'SBR', filename)
#             if sbr_match:
#                 sbr_fn = sbr_match.group(0).lower()  # Convert OI-SOS to lowercase
#                 if sbr_fn == fn_filter:
#                     data.append((fy, qtr, sbr_fn, 'SBRRealigned', filename))
                    
            

#     # Create a DataFrame from the list of tuples (FY, QTR, FN, Tab, File Name)
#     df = pd.DataFrame(data, columns=['FY', 'QTR', 'FN', 'Tab', 'File Name'])

#     # Now, load the Excel file using the file name and the sheet name (Tab)
#     dfs = []  # This will hold all DataFrames that match the file name and tab
#     for _, row in df.iterrows():
#         file_path = os.path.join(directory_path, row['File Name'])
#         sheet_name = row['Tab']
        
#         # Use flexible sheet name matching to handle trailing spaces, case changes, etc.
#         try:
#             actual_sheet = ru.find_sheet_name(file_path, sheet_name)
#             if actual_sheet != sheet_name:
#                 logger.info("Sheet name normalised: '%s' -> '%s' in %s",
#                             sheet_name, actual_sheet, file_path)
#             sheet_df = pd.read_excel(file_path, sheet_name=actual_sheet)
#             if sheet_df.empty:
#                 logger.warning("Sheet '%s' in %s is empty.", actual_sheet, file_path)
#             dfs.append(sheet_df)
#         except ValueError as e:
#             logger.error("Sheet lookup failed for '%s' in %s: %s", sheet_name, file_path, e)
#         except Exception as e:
#             logger.error("Error loading file %s, sheet %s: %s", file_path, sheet_name, e)

#     # Combine all DataFrames into a single one (optional)
#     final_df = pd.concat(dfs, ignore_index=True) if dfs else pd.DataFrame()

#     if final_df.empty:
#         logger.warning("import_fs('%s') returned an empty DataFrame. Check file paths and sheet names.", fn_filter)

#     return final_df


def import_fs(fn_filter):
    directory_path = dir_p.fs_path()
    
    df_config = pd.read_excel(mapping_file, sheet_name='Config-FS')
    fs_config = dict(zip(df_config['report_key'].str.strip(), df_config['source_tab'].str.strip()))
    
    data = []
    for filename in os.listdir(directory_path):
        full_path = os.path.join(directory_path, filename)
        if os.path.isfile(full_path):
            fy_qtr_match = re.search(r'FY(\d{2})\s*PD(\d{1,2})', filename)
            if fy_qtr_match:
                fy = fy_qtr_match.group(1)
                qtr = fy_qtr_match.group(2)
            else:
                fy, qtr = None, None

            for file_code, sheet_name in fs_config.items():
                match = re.search(file_code, filename)
                if match:
                    fn = match.group(0).lower()
                    if fn == fn_filter:
                        data.append((fy, qtr, fn, sheet_name, filename))
                        break

    df = pd.DataFrame(data, columns=['FY', 'QTR', 'FN', 'Tab', 'File Name'])

    dfs = []
    for _, row in df.iterrows():
        file_path = os.path.join(directory_path, row['File Name'])
        sheet_name = row['Tab']

        try:
            actual_sheet = ru.find_sheet_name(file_path, sheet_name)
            if actual_sheet != sheet_name:
                logger.info("Sheet name normalised: '%s' -> '%s' in %s",
                            sheet_name, actual_sheet, file_path)
            sheet_df = pd.read_excel(file_path, sheet_name=actual_sheet)
            if sheet_df.empty:
                logger.warning("Sheet '%s' in %s is empty.", actual_sheet, file_path)
            dfs.append(sheet_df)
        except ValueError as e:
            logger.error("Sheet lookup failed for '%s' in %s: %s", sheet_name, file_path, e)
        except Exception as e:
            logger.error("Error loading file %s, sheet %s: %s", file_path, sheet_name, e)

    final_df = pd.concat(dfs, ignore_index=True) if dfs else pd.DataFrame()

    if final_df.empty:
        logger.warning("import_fs('%s') returned an empty DataFrame. Check file paths and sheet names.", fn_filter)

    return final_df

if __name__ == "__main__":
    


    # Combine folder path with filename
    fn = os.path.join(dir_p.test_path(),"rawFootnote.csv")
    fs = os.path.join(dir_p.test_path(),"rawfinancialstatement.csv")

    
    import_fs('bs').to_csv(fs, index=False)
    
    import_fn('fn30scnp').to_csv(fn, index=False)
    
    
    
    
    #cProfile.runctx("updateFile()", globals(), locals())
