' Wait a moment to ensure Python has finished writing
    Application.Wait Now + TimeValue("0:00:02")
    
    ' Check log file size
    WriteLog "Log file size after Python: " & FileLen(logPath) & " bytes"
    
    ' Print first 20 lines of log
    Dim checkFile As Integer
    Dim checkLine As String
    Dim lineCount As Long
    lineCount = 0
    checkFile = FreeFile
    Open logPath For Input As #checkFile
    Do While Not EOF(checkFile) And lineCount < 20
        Line Input #checkFile, checkLine
        WriteLog "LOG: " & checkLine
        lineCount = lineCount + 1
    Loop
    Close #checkFile
