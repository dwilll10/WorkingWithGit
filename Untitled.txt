Sub ExportOI_SOS()
    Dim pythonPath As String
    Dim scriptPath As String
    Dim logPath As String
    Dim configPath As String
    Dim command As String
    Dim wsh As Object
    Dim oneDrivePath As String
    Dim wsData As Worksheet
    Dim wsConfig As Worksheet
    Dim wbConfig As Workbook
    Dim exitCode As Long
    Dim args As String
    Dim placeholder As String
    Dim section As String
    Dim subtitle As String
    Dim value As String
    Dim lastRow As Long
    Dim i As Long
    Dim sectionRow As Long
    Dim subtitleRow As Long
    Dim nextSectionRow As Long
    Dim foundCell As Range
    Dim nextCell As Range
    Dim j As Long

    oneDrivePath = Environ("OneDrive")
    pythonPath = "C:\ProgramData\Anaconda3\python.exe"
    scriptPath = oneDrivePath & "\Documents\z. Python Program\GK_new_format\WordExportOISOS.py"
    logPath = Environ("USERPROFILE") & "\Documents\error_log_wordexport.txt"
    configPath = oneDrivePath & "\Documents\z. Python Program\GK_new_format\config.xlsx"

    ' Reference data sheet in the already open workbook
    Set wsData = ThisWorkbook.Sheets("OI-SOS")

    ' Open config workbook silently, read, then close
    Application.ScreenUpdating = False

    On Error GoTo ConfigError
    Set wbConfig = Workbooks.Open(configPath, ReadOnly:=True)
    On Error GoTo 0

    Set wsConfig = wbConfig.Sheets(1)

    MsgBox "Config sheet name: " & wsConfig.Name & vbNewLine & _
           "A1: '" & wsConfig.Cells(1, 1).Value & "'" & vbNewLine & _
           "A2: '" & wsConfig.Cells(2, 1).Value & "'" & vbNewLine & _
           "C2: '" & wsConfig.Cells(2, 3).Value & "'" & vbNewLine & _
           "lastRow: " & wsConfig.Cells(wsConfig.Rows.Count, "A").End(xlUp).Row, _
           vbInformation, "Config Check"

    ' Find last row of config table
    lastRow = wsConfig.Cells(wsConfig.Rows.Count, "A").End(xlUp).Row

    ' Build args by iterating config rows (skip header in row 1)
    args = ""
    For i = 2 To lastRow
        section = Trim(wsConfig.Cells(i, 1).Value)
        subtitle = Trim(wsConfig.Cells(i, 2).Value)
        placeholder = Trim(wsConfig.Cells(i, 3).Value)

        MsgBox "Row " & i & ": section='" & section & "' subtitle='" & subtitle & "' placeholder='" & placeholder & "'", vbInformation, "Config Read"

        If section = "" Or subtitle = "" Or placeholder = "" Then
            MsgBox "SKIPPING row " & i & " - one or more fields empty", vbExclamation, "Skipped"
            GoTo NextRow
        End If

        ' Step 1: find section in col A of OI-SOS
        Set foundCell = wsData.Columns("A").Find( _
            What:=section, LookIn:=xlValues, LookAt:=xlWhole, MatchCase:=False)

        If foundCell Is Nothing Then
            MsgBox "SECTION NOT FOUND: '" & section & "'", vbExclamation, "Step 1 Failed"
            GoTo NextRow
        End If

        sectionRow = foundCell.Row
        MsgBox "Section '" & section & "' found at row " & sectionRow, vbInformation, "Step 1 OK"

        ' Step 2: find next section to bound the search
        Set nextCell = wsData.Columns("A").FindNext(foundCell)
        If nextCell Is Nothing Or nextCell.Row <= sectionRow Then
            nextSectionRow = wsData.Cells(wsData.Rows.Count, "C").End(xlUp).Row
        Else
            nextSectionRow = nextCell.Row - 1
        End If

        MsgBox "Searching col C from row " & sectionRow & " to " & nextSectionRow, vbInformation, "Step 2"

        ' Step 3: find subtitle in col C within the bounded range
        subtitleRow = 0
        For j = sectionRow To nextSectionRow
            If Trim(wsData.Cells(j, 3).Value) = subtitle Then
                subtitleRow = j
                Exit For
            End If
        Next j

        If subtitleRow = 0 Then
            MsgBox "SUBTITLE NOT FOUND: '" & subtitle & "'" & vbNewLine & _
                   "Col C values searched:" & vbNewLine & DebugColCValues(wsData, sectionRow, nextSectionRow), _
                   vbExclamation, "Step 3 Failed"
            GoTo NextRow
        End If

        MsgBox "Subtitle '" & subtitle & "' found at row " & subtitleRow, vbInformation, "Step 3 OK"

        ' Step 4: read value from col D of that row
        value = wsData.Cells(subtitleRow, 4).Value
        MsgBox "About to append: " & placeholder & "=" & value, vbInformation, "Step 4"
        args = args & " """ & placeholder & "=" & value & """"
        MsgBox "Args now: " & args, vbInformation, "Args"

NextRow:
    Next i

    ' Close config workbook without saving
    wbConfig.Close SaveChanges:=False
    Application.ScreenUpdating = True

    MsgBox "Final Args: " & args, vbInformation, "Debug"

    command = """" & pythonPath & """ """ & scriptPath & """" & args & " > """ & logPath & """ 2>&1"

    MsgBox "Command: " & command, vbInformation, "Debug"

    Set wsh = CreateObject("WScript.Shell")
    Debug.Print "Command: " & command
    exitCode = wsh.Run("cmd /c """ & command & """", 0, True)

    If exitCode = 0 Then
        MsgBox "Word document generated successfully!", vbInformation, "Done"
    Else
        MsgBox "Script failed. Check log: " & logPath, vbCritical, "Error"
    End If

    Set wsh = Nothing
    Exit Sub

ConfigError:
    Application.ScreenUpdating = True
    MsgBox "Could not open config file: " & configPath, vbCritical, "Config Error"
    Exit Sub

End Sub


Function DebugColCValues(ws As Worksheet, startRow As Long, endRow As Long) As String
    Dim result As String
    Dim k As Long
    Dim cellVal As String
    For k = startRow To endRow
        cellVal = ws.Cells(k, 3).Value
        If Trim(cellVal) <> "" Then
            result = result & "Row " & k & ": '" & cellVal & "'" & vbNewLine
        End If
    Next k
    DebugColCValues = result
End Function
