WriteLog "phTabMap sample entries:"
    Dim sampleKey As Variant
    Dim sampleCount As Long
    sampleCount = 0
    For Each sampleKey In phTabMap.Keys
        If sampleCount < 10 Then
            WriteLog "  '" & sampleKey & "' = " & phTabMap(sampleKey)
            sampleCount = sampleCount + 1
        End If
    Next sampleKey


ElseIf inUnreplacedSection And Left(Trim(logLine), 2) = "@@" Then
            foundPh = Trim(logLine)
            WriteLog "Looking up: '" & foundPh & "' exists=" & CStr(phTabMap.Exists(foundPh))
