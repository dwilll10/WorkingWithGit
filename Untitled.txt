' Write command to a temp batch file and run that instead
    Dim batPath As String
    batPath = Environ("USERPROFILE") & "\Documents\run_export.bat"
    
    Dim fileNum As Integer
    fileNum = FreeFile
    Open batPath For Output As #fileNum
    Print #fileNum, "@echo off"
    Print #fileNum, command & " > """ & logPath & """ 2>&1"
    Close #fileNum
    
    exitCode = wsh.Run("cmd /c """ & batPath & """", 0, True)


command = """" & pythonPath & """ """ & scriptPath & """ """ & wordTemplate & """ """ & outputPath & """" & args
