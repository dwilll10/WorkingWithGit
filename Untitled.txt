Dim sumOccurrence As Long
            sumOccurrence = 1
            If Trim(wsSums.Cells(sRow, 5).Value) <> "" Then
                sumOccurrence = CLng(wsSums.Cells(sRow, 5).Value)
            End If

            Dim sumSubRow As Long
            Dim sumMatchCount As Long
            sumSubRow = 0
            sumMatchCount = 0
            Dim sk As Long
            For sk = sumSectionRow To sumNextSectionRow
                If Trim(sumDataSheet.Cells(sk, 3).Value) = sumSubtitle Then
                    sumMatchCount = sumMatchCount + 1
                    If sumMatchCount = sumOccurrence Then
                        sumSubRow = sk
                        Exit For
                    End If
                End If
            Next sk
