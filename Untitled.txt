' Close output doc if already open in Word
Dim wdDoc As Object
For Each wdDoc In Application.Workbooks
    If wdDoc.FullName = outputPath Then
        wdDoc.Close SaveChanges:=False
    End If
Next wdDoc


import os
import sys
import zipfile
import shutil
from lxml import etree

W = "http://schemas.openxmlformats.org/wordprocessingml/2006/main"

XML_PARTS = [
    "word/document.xml",
    "word/header1.xml",
    "word/header2.xml",
    "word/header3.xml",
    "word/footer1.xml",
    "word/footer2.xml",
    "word/footer3.xml",
]


def format_value(value):
    try:
        num = float(value.replace(",", ""))
        if num < 0:
            return "(" + f"{abs(num):,.0f}" + ")"
        else:
            return f"{num:,.0f}"
    except ValueError:
        return value


def parse_args(argv):
    if len(argv) < 3:
        print("Usage: WordExportOISOS.py <word_template> <output_path> [placeholder=value ...]")
        sys.exit(1)

    word_path   = argv[1]
    output_path = argv[2]
    replacements = {}

    for arg in argv[3:]:
        if "=" in arg:
            placeholder, value = arg.split("=", 1)
            replacements[placeholder.strip()] = format_value(value.strip())

    return word_path, output_path, replacements


def replace_in_xml(xml_bytes, placeholder, value):
    tree = etree.fromstring(xml_bytes)

    for elem in tree.iter(f"{{{W}}}t"):
        if elem.text and placeholder in elem.text:
            elem.text = elem.text.replace(placeholder, value)

    for para in tree.iter(f"{{{W}}}p"):
        runs = para.findall(f".//{{{W}}}r")
        text_runs = [r for r in runs if r.find(f"{{{W}}}t") is not None]
        full_text = "".join((r.find(f"{{{W}}}t").text or "") for r in text_runs)
        if placeholder in full_text:
            new_text = full_text.replace(placeholder, value)
            if text_runs:
                text_runs[0].find(f"{{{W}}}t").text = new_text
                for run in text_runs[1:]:
                    run.find(f"{{{W}}}t").text = ""

    return etree.tostring(tree, encoding="unicode").encode("utf-8")


def fill_word(word_path, replacements, output_path):
    shutil.copy2(word_path, output_path)

    tmp_path = output_path + ".tmp"
    with zipfile.ZipFile(output_path, "r") as zin:
        with zipfile.ZipFile(tmp_path, "w", zipfile.ZIP_DEFLATED) as zout:
            for item in zin.infolist():
                data = zin.read(item.filename)
                if item.filename in XML_PARTS:
                    # ----- DIAGNOSTIC: dump raw bytes before processing -----
                    diag_before = os.path.join(
                        os.path.dirname(output_path),
                        "diag_" + item.filename.replace("/", "_") + "_BEFORE.xml"
                    )
                    with open(diag_before, "wb") as f:
                        f.write(data)
                    print(f"DIAG: wrote {diag_before} ({len(data)} bytes)")

                    for placeholder, value in replacements.items():
                        data = replace_in_xml(data, placeholder, value)

                    # ----- DIAGNOSTIC: dump raw bytes after processing -----
                    diag_after = os.path.join(
                        os.path.dirname(output_path),
                        "diag_" + item.filename.replace("/", "_") + "_AFTER.xml"
                    )
                    with open(diag_after, "wb") as f:
                        f.write(data)
                    print(f"DIAG: wrote {diag_after} ({len(data)} bytes)")

                zout.writestr(item, data)

    os.replace(tmp_path, output_path)
    print(f"\nSaved to {output_path}")
    print("Automation was successfully completed without errors")


if __name__ == "__main__":
    word_path, output_path, replacements = parse_args(sys.argv)

    print(f"Template: {word_path}")
    print(f"Output:   {output_path}")
    print(f"Replacements ({len(replacements)}):")
    for p, v in replacements.items():
        print(f"  {p} = {v}")

    fill_word(word_path, replacements, output_path)
