# Verify output is a valid zip/docx
import zipfile as _zf
try:
    with _zf.ZipFile(output_path, "r") as _z:
        bad = _z.testzip()
        if bad:
            print(f"ZIP ERROR: first bad file is {bad}")
        else:
            print(f"ZIP OK: {len(_z.namelist())} files, size={os.path.getsize(output_path)}")
except Exception as e:
    print(f"ZIP OPEN FAILED: {e}")
